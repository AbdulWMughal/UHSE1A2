import { Slot } from "expo-router"; // Import the Slot component from expo-router

// ChatLayout component to define the layout structure of the chat screen
export default function ChatLayout() {
  return <Slot />; // Render the Slot component which acts as a placeholder for nested routes
}
